# Normalize text files and keep LF line endings in the working tree
* text=auto eol=lf

# Note: Hugging Face Spaces now uses xet storage for binary files instead of Git LFS
# Binary files (PNG, JPEG, GIF, audio, etc.) are handled automatically by xet
